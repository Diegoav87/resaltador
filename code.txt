#include <stdio.h>

// This is a single-line comment

struct Point {
    int x;
    int y;
};

int add(int a, int b) {
    return a + b;
}

int subtract(int a, int b) {
    return a - b;
}

void print_greeting() {
    printf("Hello, World!");
}

int main() {
    struct Point p1;
    
    p1.x = 7;
    p1.y = 8;

    int x = 5;
    int y = 3; // Another comment

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }\

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }



if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

    if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }

if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }


if (x > y) {
        printf("x is greater than y");
    } else {
        printf("y is greater than or equal to x");
    }

    if (x > 0 && y > 0) {
        printf("Both x and y are positive numbers");
    }

    if (x < 0 || y < 0) {
        printf("At least one of x or y is a negative number");
    }

    for (int i = 0; i < 5; i++) {
        printf("i: %d", i);
    }

    while (y > 0) {
        printf("y: %d", y);
        y--;
    }




    char ch = 'A';
    printf("Character literal: %c", ch);

    print_greeting();

    int result = add(x, y);
    printf("Result of addition: %d", result);

    return 0;
}
